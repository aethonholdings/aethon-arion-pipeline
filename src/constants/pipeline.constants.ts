export enum States {
    RUNNING = "running",
    PENDING = "pending",
    COMPLETED = "completed",
    FAILED = "failed"
}
export enum DomainTypes {
    CONTINUOUS = "continuous",
    DISCRETE = "discrete",
    CATEGORICAL = "categorical",
    BOOLEAN = "boolean"
}